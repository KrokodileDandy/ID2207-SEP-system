import React from "react";

const Login = () => {
  return (
    <div>
        <h1>Login</h1>
        <p>Log in with username or password, or create a user if you do not have one already.</p>
    </div>
  );
}
export default Login;